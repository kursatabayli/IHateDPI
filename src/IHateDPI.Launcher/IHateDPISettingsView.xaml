<UserControl x:Class="IHateDPI.Launcher.IHateDPISettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="550" d:DesignWidth="360"
             Background="Transparent" Foreground="#F8F8F2">

    <UserControl.Resources>
        <SolidColorBrush x:Key="BgCard" Color="#343746"/>
        <SolidColorBrush x:Key="AccentPurple" Color="#BD93F9"/>
        <SolidColorBrush x:Key="TextMuted" Color="#6272A4"/>

        <Style x:Key="ToggleSwitch" TargetType="CheckBox">
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Foreground" Value="#F8F8F2"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Grid Margin="0,6">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <ContentPresenter Grid.Column="0" VerticalAlignment="Center"/>
                            <Border x:Name="SwitchTrack" Grid.Column="1" 
                                    Width="40" Height="22" CornerRadius="11" 
                                    Background="#44475A">
                                <Ellipse x:Name="SwitchKnob" Width="18" Height="18" Fill="White" 
                                         HorizontalAlignment="Left" Margin="2,0,0,0">
                                    <Ellipse.RenderTransform>
                                        <TranslateTransform x:Name="KnobTranslate" X="0"/>
                                    </Ellipse.RenderTransform>
                                    <Ellipse.Effect>
                                        <DropShadowEffect Color="Black" Opacity="0.3" BlurRadius="2" ShadowDepth="1"/>
                                    </Ellipse.Effect>
                                </Ellipse>
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="SwitchTrack" Property="Background" Value="#50FA7B"/>
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="KnobTranslate" Storyboard.TargetProperty="X" To="18" Duration="0:0:0.25">
                                                <DoubleAnimation.EasingFunction>
                                                    <CubicEase EasingMode="EaseOut"/>
                                                </DoubleAnimation.EasingFunction>
                                            </DoubleAnimation>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="KnobTranslate" Storyboard.TargetProperty="X" To="0" Duration="0:0:0.25">
                                                <DoubleAnimation.EasingFunction>
                                                    <CubicEase EasingMode="EaseOut"/>
                                                </DoubleAnimation.EasingFunction>
                                            </DoubleAnimation>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="SwitchTrack" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#21222C"/>
            <Setter Property="Foreground" Value="#F8F8F2"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#44475A"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="false" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource AccentPurple}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SettingsCard" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource BgCard}"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="12"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
        </Style>

        <Style x:Key="SectionHeader" TargetType="TextBlock">
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="{StaticResource TextMuted}"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Hidden" PanningMode="VerticalOnly">
            <StackPanel Margin="20,10,20,10">

                <Border Style="{StaticResource SettingsCard}">
                    <StackPanel>
                        <TextBlock Text="BAĞLANTI" Style="{StaticResource SectionHeader}"/>
                        <CheckBox x:Name="ChkDoH" Content="DNS over HTTPS (DoH)" Style="{StaticResource ToggleSwitch}"/>
                        <StackPanel Margin="0,10,0,0" >
                            <TextBlock Text="URL" VerticalAlignment="Center" Foreground="{StaticResource TextMuted}" FontSize="11" Margin="0,0,0,5"/>
                            <TextBox x:Name="TxtDohUrl" IsEnabled="{Binding ElementName=ChkDoH, Path=IsChecked}"/>
                        </StackPanel>
                        <CheckBox x:Name="ChkBlockQuic" Content="Block QUIC" Style="{StaticResource ToggleSwitch}"/>
                        <StackPanel Margin="0,10,0,0">
                            <TextBlock Text="Max Payload Size" VerticalAlignment="Center" Foreground="{StaticResource TextMuted}" FontSize="11" Margin="0,0,0,5"/>
                            <TextBox x:Name="TxtMaxPayload" />
                        </StackPanel>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource SettingsCard}">
                    <StackPanel>
                        <TextBlock Text="PARÇALAMA" Style="{StaticResource SectionHeader}"/>
                        <CheckBox x:Name="ChkReverse" Content="Reverse Fragmentation" Style="{StaticResource ToggleSwitch}"/>
                        <Grid Margin="0,5,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="HTTPS Frag" Foreground="{StaticResource TextMuted}" FontSize="9" Margin="0,0,0,2"/>
                                <TextBox x:Name="TxtFragHttps"/>
                            </StackPanel>
                            <StackPanel Grid.Column="2">
                                <TextBlock Text="HTTP Frag" Foreground="{StaticResource TextMuted}" FontSize="9" Margin="0,0,0,2"/>
                                <TextBox x:Name="TxtFragHttp"/>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource SettingsCard}">
                    <StackPanel>
                        <TextBlock Text="HTTP HEADER" Style="{StaticResource SectionHeader}"/>
                        <CheckBox x:Name="ChkMixHost" Content="Mix Host (hOsT)" Style="{StaticResource ToggleSwitch}"/>
                        <CheckBox x:Name="ChkHostNoSpace" Content="Host No Space" Style="{StaticResource ToggleSwitch}"/>
                        <CheckBox x:Name="ChkAddSpace" Content="Additional Space" Style="{StaticResource ToggleSwitch}"/>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource SettingsCard}">
                    <StackPanel>
                        <TextBlock Text="FAKE PACKET" Style="{StaticResource SectionHeader}"/>
                        <CheckBox x:Name="ChkBadSeq" Content="Bad Sequence" Style="{StaticResource ToggleSwitch}"/>
                        <CheckBox x:Name="ChkBadSum" Content="Bad Checksum" Style="{StaticResource ToggleSwitch}"/>
                        <Grid Margin="0,5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="TTL" Foreground="{StaticResource TextMuted}" FontSize="9" Margin="0,0,0,2"/>
                                <TextBox x:Name="TxtFakeTTL"/>
                            </StackPanel>
                            <StackPanel Grid.Column="2">
                                <TextBlock Text="Count" Foreground="{StaticResource TextMuted}" FontSize="9" Margin="0,0,0,2"/>
                                <TextBox x:Name="TxtFakeCount"/>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>
                <Border Height="20"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>